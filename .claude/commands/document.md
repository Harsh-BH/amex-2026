---
description: Update project documentation, especially the graded Profitability Framework writeup and memory.
argument-hint: [what to document, e.g. "v3 framework" or "log today's decision"]
---
Invoke the **documentation** skill.

Target: $ARGUMENTS

**Expected output:** the right artifact updated — `templates/framework-doc.md` (must match `scoring.py`), `memory/decision-log.md`, `assumptions.md`, `feature-notes.md`, or a `report.md`. Write *why*, not just *what*.

**Internal workflow:** pick the home → write tied to business rationale → ensure writeup ↔ code consistency → prune stale claims.
