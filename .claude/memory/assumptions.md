# Assumptions Register

_Every assumption we rely on. Status: ✅ verified · ⚠ unverified · ❌ disproven. Re-check before trusting._

| # | Assumption | Status | Evidence / Action |
|---|---|---|---|
| A1 | `f5` "Total Spend" is **NOT** the sum of `f6`–`f10` | ✅ | `f5` max ≈13,596 but `f7` max ≈146,701. Different scale/definition. Do not assume additivity. |
| A2 | `f6`–`f10` (category spends) are missing **together** for the same 115,698 rows | ✅ | Identical missing counts across all five in the 500K scan. Likely "no category breakdown" segment. |
| A3 | `f4` (rewards balance) & `f21` (redeemed) co-miss for the same 257,228 rows | ✅ | Identical missing counts → a "rewards population" flag. |
| A4 | `f17`/`f18` missing (~58/62%) = charge-only members with no lend line | ⚠ | Plausible; confirm distribution vs revolve `f1`. |
| A5 | `f11` is a probability-of-default-like risk score (higher = riskier = cost) | ⚠ | Range 0–0.326, mean 0.034. Confirm monotonic direction in EDA. |
| A6 | `f7` negatives are refunds/returns (legitimate, keep sign) | ⚠ | Min −274.6. Decide treatment (floor at 0? net?) in framework design. |
| A7 | Annual fee is constant within Premier → not a ranking feature | ✅ | Stated by product overview; fee is product-level. |
| A8 | Ground truth profitability ≈ a revenue−cost formula on these (or richer) features | ⚠ | Inference from problem design; validated only via leaderboard. |
| A9 | `Prediction` should be a continuous profitability score (rank-ordered), not a 0/1 flag | ⚠ | Metric ranks our values to cut top-20%; continuous is safer. Confirm template accepts floats. |
| A10 | Tenure / Bureau score / Size-of-Wallet (slide 9) are **absent** from the 23 features | ✅ | Dictionary has only `f1`–`f23`; slide is illustrative. |
| A11 | `f17`/`f18` line SIZE is a **capital COST, not revenue** (unused line: $0 income, but CECL provisioning + Basel III RWA capital drag ≈ −0.3 to −0.5%/$) | ✅ | **3-source convergent** (internal + external + Gemini 10-K analysis). Lending profit is the carried balance `f1` (`balance_int`), NOT the line. → `borrow_int` should be **0 or negative**, not +0.40. v1.2 zeroes it. Stage-9 2026-06-29. |
| A14 | **`f5` is UNCORRELATED with category-sum spend (Spearman 0.009) and with profitability (−0.038)** — NOT a usable spend-volume signal | ✅ | Measured on 500K. Contradicts the "Total Spend" decode as a volume proxy (capped ~13.6K; different scale/window/def). ⇒ the ~23% no-breakdown cohort has NO usable spend signal; best proxy is `f4` (Spearman 0.28, only 35% have it). A major ranking ceiling — likely part of the 0.733→0.90 gap. (v7 cohort-fix.) |
| A13 | Spend-category net margins are **asymmetric; airline `f6` & lodging `f9` are net-NEGATIVE** (5x reward cost ~4.5% > MDR ~2.5–3%); dining `f10` & other `f7` net-positive | ⚠ | Cited MDR/reward rate cards (Gemini). Summing `f5`/`f6`–`f10` mixes profit+loss. But internal test: margin-weighting re-ranks <7% of top-20% (Spearman 0.998). Writeup-relevant; low ranking impact. |
| A12 | Amex rewards **breakage is LOW** (~3–4%; Membership-Rewards URR ≈96%) | ⚠ | Cited (Amex MR liability disclosure). **Contradicts the generic 17–18% breakage prior** — `f4` points balance is a near-FULL liability, not 80–90% discounted. Tighten the `f4` discount for Amex. Stage-9. |
| A15 | The ~23% no-breakdown cohort's spend is **recoverable**: a linear imputation from `f4`+`f21`+benefit-usage(`f13–f16`)+risk `f11` predicts category-sum spend at **OOS Spearman 0.527** vs **0.282 for `f4` alone** (5-fold CV, 500K). v7 uses `f4`-or-zero, **zeroing 75,174 members (15% of pop; 38% of them also have `f1`=0 → near-random rank)**. The imputation's within-cohort ORDERING is real (OOS 0.46 >> 0.19), BUT promoting the cohort HURTS the metric. | ⚠ ordering-yes / promotion-NO | data-explorer 2026-06-29. **v9 BUILT + uploaded → public LB 0.727 (−0.041 vs v7).** The imputation raised the no-breakdown cohort from 10.5%→19.0% of the top-20% and that LOWERED overlap → **missing spend-breakdown is itself a NEGATIVE profitability signal; the cohort belongs mostly outside the top-20%, and v7's demotion was right.** The 0.768→0.90 gap is NOT here. Do not promote this cohort; if anything, demote further. |
| A16 | The **negative airline/lodging margins were NEVER isolated/validated.** The +0.119 v3→v5 jump bundled 3 changes (flat→per-category, drop `f21`, negative sign); v5→v7 (+0.035) was 100% cohort-fix (`CAT_MARGIN` byte-identical); both clean tests of "more negative" LOST (v6 −0.058, v8 −0.087). Against plausible "spend = positive interchange" truths an all-positive variant beats v7 by 0.07–0.13. **Likely a public-70% local max → the top private-LB risk.** | ⚠ | framework-critic 2026-06-29. De-risk: flip to all-positive, KEEP per-category asymmetry (that structure is what won, not the sign). Supersedes the strategic read of A13. **REFINED (2026-06-29, framework-critic + `verify_critic.py`): do NOT flip to all-positive.** A +0.137 dining FLIP rides the ceiling-CENSORED dining cluster (25.2% of promoted members pinned at the data cap vs 5.5% pop; dining 8× pop median) = a public-70% local max, AND a positive flip contradicts the re-fit's "signs are noise". The v7(0.768)>v8(0.681) evidence is **~65% confounded** (v7→v8 moved all 5 margins; dining-only explains ~65% of the reshuffle). **Defensible de-risk = NEUTRALIZE f10/f6 to 0 (`dollar_profit_v12_signfix`), NOT flip** — and only as a 2nd bet after v11-minimal's LB read. |
| A17 | **`f3`=1 (collection-cancel) members contaminate the top-20%:** 6,662 land in v7's top-20% despite 10× default risk (`f11`=0.171) and ~$0 spend ($392) — their high `f1` interest credit beats the weak `exp_loss` term. `f2`=1 (cancel calls) marks low-spend low-risk transactors (already mostly demoted). | ✅ | data-explorer 2026-06-29. Add an explicit `f3` penalty / cap delinquent interest credit. **FIX BUILT (v11, 2026-06-29, [[experiment-history]]):** evicting f3=1 from v10's top-20% removes all 4,092 contaminants AND drops no-breakdown share 5%→1.1% — **they are the SAME members**, so A17 contamination ≈ the no-breakdown over-inclusion; one surgical eviction (`dollar_profit_v10_f3demote`) fixes both, Jaccard 0.921 vs v10. **LB-VALIDATED 2026-06-29: v11-minimal (this exact fix) scored 0.823, +0.018 over v10's 0.805 — WON, new best.** The distress-demotion lever is real: the truth penalizes the no-spend/collection-flagged tail more than v10 did. |
| A18 | **`f19`/`f20` (supp accounts / active cards) are NEGATIVELY correlated with spend** — `f19` Spearman −0.214 within breakdown cohort (`f19`=1 → 57K spend vs `f19`=4 → 32K). **Never use `f19`/`f20` as a revenue multiplier.** | ✅ | data-explorer 2026-06-29 — refutes the intuitive "more cards = more spend" prior (and a framework-critic suggestion). |

| A19 | **At 0.823 the top-20% cutoff is a DENSE boundary — no feature-based lever remains.** Members just-IN (rank 90–100K) vs just-OUT (100–110K) are near-identical on every f1–f23 feature (catspend 67K vs 62K, balance 589 vs 573, risk 0.000 both, logins 19 both, lend% 39% both, no-spend ~2% both); NO feature is systematically higher in the just-OUT band → nothing is under-weighted. | ✅ | `scratchpad/boundary.py` 2026-06-29. The 0.823→0.90 gap is in a region the feature set cannot separate; the tie-breakers would be the ABSENT features (tenure/size-of-wallet/bureau, A10). Further gains need more LB signal or genuinely new structure — NOT re-weighting. **Don't spend submissions chasing the boundary; v11-minimal (0.823) is the practical ceiling for f1–f23 + this framework.** |
| A20 | **Feature engineering on f1–f23 is EXHAUSTED at the 0.823 boundary — no derived feature is a confident lever.** 23+ derived features tested (ratios, log/sqrt, HHI/entropy, interactions, gates); none shows >10% systematic median diff between just-IN (rank 90–100k) and just-OUT (100–110k). Largest raw gap (catspend $4,776) = ~0.003 score units vs the 0.1424 cutoff gap. "Promising" ratios are denominator artifacts (logins/spend moves 16 members), redundant (spend×f7share r=0.998 w/ f7), or near-zero at the boundary (redemption_intensity direction inverts at w≥0.05). | ✅ | data-explorer 2026-06-29 (`scratchpad/`). Re-weighting/engineering f1–f23 cannot bridge 0.823→0.90. |
| A21 | **0.823 is the CERTIFIED ceiling recoverable from our 11 LB constraints — proven via LP, not estimated.** The 11 submissions partition 500K members into only **884 distinct membership-patterns**; the LB observes ONLY this partition → within-cell ordering is invisible to it → rank-fusion & per-member-probability (membership-based) CANNOT beat v11min (every fusion's LP-guaranteed true-LB < 0.823). Non-linear triangulation (+f7×(1−f11), f1×(1−f11), spend-HHI, no-bd flag) fit WORSE (in-sample RMSE 0.0017 vs linear 0.0010) despite +4 params → no non-linear LB signal, just overfit. Best per-member-prob set est +0.009 but LP true-LB interval [0.777, 0.871] (floor BELOW v11min) and < private-30% LB noise sd ≈0.0085. | ✅ | general-purpose reverse-eng 2026-06-29 (`scratchpad/00–18`). **Resolves f2:** #4's f2~LB Spearman −0.945 is CONFOUNDED (n=11, tracks early f5-submission quality); per-member-prob shows v11min's members beat v7's dropped outsiders (truth-prob 0.517 vs 0.253) → no swap helps; f2=1 members in the top-20% are genuine high-spenders (median 88k) → f2 is NOT a lever. |
| A22 | **Four adversarial segment-rule stress-tests (2026-06-30, R1–R4) all confirm 0.823 is the practical ceiling AND that v11min is economically CORRECT — every plausible "demote the unprofitable" rule either HURTS or is sub-noise.** R1 travel-arbitrageur (3,766 in top-20%, travel-share≥0.5 & catrev≤0): KILL — carried by revolving-balance INTEREST (median f1 $17,429, ~97th pct), total margin $974 > the $762 of would-be replacements → demotion HURTS (f9-lodging-blindness a red herring, only 58 pure cases). R2 benefit-parasite (374): KILL — benefit cost only 20–25% of catrev+interest, never dominant; swap −$36 worse; benefit users slightly MORE represented at top (ρ +0.065). R3 attrition-interaction (31): KILL — high-revolve no-spenders correctly valued by interest (net $1,366 > $793); A21's "f2 not a lever" holds unqualified. R4 risky-revolver (430 net-negative; high-f1 × f11>0.10 × f3=0): swap PASSES economically (+$968/member) but SUB-NOISE — 430–887 members, swap-boundary score gap 0.0007, max overlap gain ~0.002–0.009 ≤ private-LB noise sd 0.0085. | ✅ | data-explorer ×4 (2026-06-30). **The 1,780 net-negative top-20% members decompose into Group A (1,383 catrev-negative airline/lodging spenders — the A16 category-sign issue) + Group B (430 risky revolvers — the risk term −0.110·z(f11·f1) offsets only 28% of the f1 boost).** Both are real, documentable robustness seams but sub-noise to fix → **DECISION: HOLD submissions, bank 0.823 ([[decision-log]] 2026-06-30).** Defensible private-30% hedges identified for the WRITEUP only (not a public-LB lever): risk-floor (demote net-negative lending, expected loss > interest ⇔ f11>0.16 — the parameter-free generalization of the validated f3-evict) + category-sign neutralization (v12, addresses Group A). |
| A23 | **The gap to the 0.906–0.915 leaders is FUNCTIONAL FORM, not features/weights/absent-data — and v11min's "dense boundary" (A19) is form-specific.** Leaders hit ~0.91 on the same 23 features (external data + id forbidden), so ~0.91 is reachable; the "ceiling/absent-features" read (A10/A19/A21) was over-generalized. A different defensible form (raw-dollar spend+interest−loss) reshuffles **64.6%** of v11min's rank 90K–110K — A19 only shows no feature is mis-weighted WITHIN v11min's z-score form, not that f1–f23 can't separate those members; A21's LP proof bounds the 11-submission membership-partition, not the features. **Caveat: with NO label a new form is only equally-defensible, not known-better; the one LB-tested alternative (raw-dollar v3–v7) capped at 0.768 < 0.823 → chasing the gap is a high-variance LB bet.** | ✅ | general-purpose build + framework-critic + data-explorer (2026-06-30). **Also certified:** f5 is orthogonal to value WITHIN the no-breakdown cohort (block-demotion correct — NB max interest $1,437 < cutoff $1,237, only ~340 qualify); a fuller all-feature dollar model only re-ranks via the LB-rejected negative travel margins (A16). |

| A24 | **The OBVIOUS non-linear forms (concavity, multiplicative risk, transactor/revolver split) do NOT beat v15 (0.827) — they are either LINEAR-equivalent or move in LB-unsupported directions.** Re-triangulation with v11min+v15 added CANNOT reproduce v15 (implied 0.815, LOO 0.790) and recovers NEGATIVE travel signs the LB thrice rejected → the recovered linear form is not submittable; `v16_retri` deliberately NOT built. Of the 4 NL prototypes: **segment is algebraically v15** (transactors have f1=0 so the lending terms are already 0; Jaccard 1.000, 6 movers from an f7-clip — presenting it as structural in the writeup = misrepresentation); **riskqual is near-flat** (f11 mean 0.034, gate≈1 for 89%; Jaccard 0.971; re-confirms A22); **sqrt's sqrt-then-z mechanism shaves the validated f1 lever** (Jaccard 0.900, demotes higher-f1 members, rides the ~2.6%/cat ceiling-censored A16 trap); **log is HURTS-direction** (revolver share 54%→65%, demotes the spend-transactors v15 rewarded, doubles the cutoff knife-edge band). Convex/interaction/geometric probes all diverge only by demoting magnitude-spenders or convex-extrapolating without economic basis. | ✅ | re-triangulation + framework-critic + leaderboard-strategist (2026-06-30), all numbers reproduced. **HOLD all four; EV ≈ 0 or negative; none breaks the ~0.85 linear ceiling upward.** The one untested honest reach = spend×low-risk multiplicative interaction on RAW proxy-dollars with ceiling-capped members winsorized first (economics-driven reshuffle, not a data-cap artifact). Functions live in `src/score_magnitude.py` as documented diagnostics (do-not-submit annotated). |

| A25 | **The learned-model (GBM) paradigm — the one non-formula approach (user-requested Option A) — was COMPLETED and confirms NO non-linear/interaction lever beyond v15 (0.827).** (`scratchpad/gbm_swing.py`, HistGradientBoosting, NaN-native, 2026-06-30.) **Decisive interaction diagnostic:** an additive (no-interaction) GBM vs a full-interaction GBM rank near-identically (Jaccard **0.90** on the economic target, **0.84** on the LB-vote target) → modeled interactions add ~nothing, and where they DO act they raise the no-breakdown share (2.0%→3.8%) = the v9 (−0.041) over-promotion failure direction. GBM on the economic P&L target ≈ the RAW-DOLLAR family (Jaccard 0.54 vs v15) = historically-worse (v5/v7 ≤0.768). GBM on the LB-recovered truth-vote (the only NON-circular target) is bounded by A21 + over-promotes the no-bd cohort. **No positive-EV GBM candidate → the interpretable formula was the correct tool; 0.827 is the practical ceiling for f1–f23 without a label. BOTH the formula family AND the learned-model paradigm are now exhausted.** | ✅ | 2026-06-30, completing Option A. |

| A26 | **Amex deep-research (2026-06-30, 10-K/Fed-cited, `tasks/w5uc030x9.output`) → the #1 GAP is that we UNDER-weight revolving balance f1.** CONFIRMED vs primary sources: (a) **net interest yield on Card Member loans = 11.9% FY24 / 11.5% / 10.6% (3-0 verified)** — our R_INTEREST=0.08 understates lending revenue per $ by ~⅓–½; (b) discount rate ~2.27% (3-0) → our 0.022 interchange is RIGHT; (c) net write-off only 2.0%, 30+DPD 1.3% (2-1) → credit loss is SMALL, our 0.50·risk·balance term is calibrated OK on the mean (f11̄ 0.034×0.5≈1.7%≈write-off) but should not be heavier; (d) travel/T&E discount rate is AT-OR-ABOVE average, retail is lower (2-1) → v15's all-positive travel was right, "thinner" is unsupported; (e) MR breakage ~5% URR 95% (3-0) → can't discount the points liability. Fed FEDS + revolver/transactor lit (primary, errored-only-on-session-limit, not refuted): **interest ≈ 80% of card profit, interchange net of rewards ≈ 0 on general cards, purchase volume DECOUPLED from profit (transactors = 40% of spend but revolvers drive profit).** ⇒ our f7-spend-dominant (0.738) / f1 (0.523) ordering likely has the ratio backwards vs the true economics. **Directionally confirmed by our OWN history: v10's f1 up-weight 0.08→0.52 WON (+0.037, 2nd-biggest jump).** Triangulation's f1≈0.52 is NOT evidence against higher — it's bounded by our explored range (A21). **HYPOTHESIS (testable, the freshest lever): the truth weights revolving balance higher than v15 does — raising f1 toward co-dominant/dominant with f7 is the most research-grounded untested bet for the 0.827→0.91 gap.** ⚠ guard: must not over-promote the no-breakdown cohort (v9 −0.041). | ⚠ test | 2026-06-30 deep-research; build + characterize before any submission. |

| A27 | **2nd Amex deep-research round (2026-06-30, verifier infra failed on rate-limit → claims UNVERIFIED but citation-backed; `tasks/wmz9marwp.output`) surfaced NO confident new lever past v19 (0.859).** Candidates screened via the validated LB-predictor (`src/lb_predict.py`) + orthogonality: **(in-space, predictor trustworthy)** spend-diversity/entropy, #categories, supp×spend → all best-w 0 (no gain); **dining up-weight** (claim: Visa restaurants highest interchange) → HURTS −0.030/−0.052 (re-confirms the category-reorder-toward-dining/travel direction loses); **(orthogonal, predictor BLIND = tenure-style coin-flips)** utilization f1/f17, engagement f12+f22, relationship-depth f19+f20 → all flat; **G&S/everyday-spend share** (f7+f8+f10)/catsum → +0.0094 proxy at w+0.30 (r=0.49, partly-visible) = the ONLY positive, but WITHIN the ±0.020 noise floor → not a confident win, though better-set-up than tenure (positive & partly-validated vs tenure's negative & pure-blind). Durable (writeup-grade) facts extracted: Amex Q3'24 discount rev $8.78B > NII $4.0B > fees $2.17B (spend is the largest consolidated revenue line — consistent with v19 lending CO-equal not dominant, i.e. why v20 lost); G&S (everyday) = 73% of billed business vs T&E 27%, airline only 6% (everyday spend is the durable profitable share — supports the low travel weights); T&E carries at-or-above-avg discount rate (GAO); engagement→retention is Gallup-style/indirect, no behavioral linkage. **CONCLUSION: the lending recalibration (v19) was the real find; the well is dry — every post-v19 probe (v20, tenure, GBM, segments, this round) is within-noise or a coin-flip that the tenure LB-loss says tends to lose. HOLD v19 (0.859); fold the durable economics into the writeup.** | ✅ screened | 2026-06-30. |
| A28 | **H2 (true two-population revolver/transactor model with a tunable cross-segment MERGE) is DEAD — v19's plain additive form already merges the segments OPTIMALLY.** Tested the merge as a knob, screened via the validated LB-predictor (`scratchpad/h2_segment.py`): **(H2a, flat segment offset δ)** proxy peaks EXACTLY at δ=0 (=v19, revolvers 58.7% of top-20%) — pushing the mix toward more revolvers (60/62/66%) OR more transactors (57/54/50%) BOTH strictly lower the proxy; clean inverted-V centered on v19, best candidate = v19 itself at +0.0000. **(H2b, two-model within-segment z-standardize + merge θ)** EVERY setting worse than v19 (even θ=0 is −0.0087) — within-segment standardization discards the cross-segment MAGNITUDE that wins (wrongly equalizes a top transactor with a top revolver). **Mechanism:** the boundary band (rank 90–110K) is 52.7% revolver ≈ the population mix, and the in-side (53.1%) vs out-side (52.3%) differ by 0.8pp → the truth's cutoff is genuinely segment-MIXED, no "revolvers in / transactors out" signal exists. v19's f1 sweep (0.523→0.738) already set the segment boundary where the truth wants it; the orthogonal flat-offset knob re-confirms it. ⇒ the most under-explored lever I'd flagged is already solved implicitly by the additive form. Consistent with A24 (segment model ≈ v15-or-worse). | ✅ | 2026-07-01, `scratchpad/h2_segment.py`. **HOLD v19. The cross-segment merge is not a lever.** |
| A29 | **H1 (the dropped COST side as a boundary tie-breaker) is DEAD — the truth does NOT penalize cost at the top-20% boundary.** Hypothesis: at the dense cutoff, revenue terms saturate so rewards-burn (f21) + benefit-credits (f13–f16) should break ties. Screened 3 forms via the LB-predictor (`scratchpad/h1_costside.py`): **H1a** absolute rewards cost → peak at w=0 (any subtraction hurts, −0.006 at w=0.10); **H1b** absolute benefit cost → peak w=0.05 at +0.0004 (50× below the 0.020 noise floor = flat, then hurts); **H1c** cost-per-$ burden (orthogonal-to-spend ratio) → peak at w=0. ALL three say "subtract nothing." Re-confirms the spine of the whole project (v6 f4-liability LOST −0.058, v8 aggressive-margins LOST −0.087, A22 benefit-rules sub-noise): **the truth rewards revenue MAGNITUDE and does not net out cost at the margin** — the cost side is real economics for the writeup but NOT a ranking lever. ⇒ both structurally-new defensible levers I had (H1 cost, H2 merge) are now empirically dead; combined with v20/v21/GBM/A24/A27 the f1–f23 lever space is exhausted as far as the validated predictor (LOO Spearman 0.945) can see. Remaining live explanations for the 0.859→0.91 gap: sharper feature DECODING (untested), public-LB overfit (won't hold private), or a ranking principle outside our hypothesis space. | ✅ | 2026-07-01, `scratchpad/h1_costside.py`. **HOLD v19; stop probing re-weights — pivot to writeup/R2/R3 + verify 0.859 is on the placement account.** |
| A30 | **H4 (f5 carries a hidden wallet/off-category signal as a RESIDUAL) is DEAD AT THE ROOT — f5 is a genuinely degraded measure, not a richer total.** Tested whether the part of f5 orthogonal to the category breakdown (f6–f10) is a usable signal. `scratchpad/h4_f5residual.py`: OLS f5~categories on 380K rows has **R²=0.023** (f5 barely relates to the spend we trust) → the residual is ~all of f5, and **corr(residual, value)=+0.013** = pure noise, same as raw f5 alone (r=0.009, A14/A15). All 3 forms (residual, raw f5, off-category share) peak at w=0; any weight either sign hurts. ⇒ f5 confirmed structurally uninformative even decoded as a residual — if the 0.91 leaders have a feature-decode edge, it is NOT on f5. Three free screens this session (H1 cost, H2 merge, H4 f5-decode) all dead; with v20/v21/GBM/A24/A22/A27 the f1–f23 lever space is now comprehensively mapped and exhausted vs the validated LB-predictor. **The 0.859→0.91 gap is most likely public-LB overfit (won't survive the private 30%) or a structural insight outside our hypothesis space — NOT a re-weighting we have missed.** | ✅ | 2026-07-01, `scratchpad/h4_f5residual.py`. **STOP probing. v19 (0.859) is the confirmed ceiling. Highest-value actions: (1) verify 0.859 is on the placement account; (2) make the writeup + R2/R3 the edge.** |

| A31 | **H3 (the PRECISE per-member dollar P&L, exact card-economics rates from the brief slide 8) does NOT beat v19 — and the prior margin-sign losses (v6 -0.058, v8 -0.087) were misattributed: the dominant failure is EQUAL-WEIGHTING the P&L terms, not the travel-penalty SIGN.** Built the literal revenue-cost P&L (interchange 2.2%, interest 12%, reward cost 5x-travel/1x-other @1.5c/pt, benefits, expected loss) and screened via the LB-predictor (`scratchpad/h3_exacteconomics.py`, `h3_decompose.py`, `h3_isolate_v2.py`). **Pure $ P&L: proxy -0.41 vs v19, Jaccard only 0.315** — catastrophically worse, an order of magnitude past any prior margin test. Decomposition isolated WHY: an all-positive (no travel penalty) version on the SAME equal-weighted raw-$ or z-scored basis is STILL -0.15 to -0.23 vs v19 (`h3_decompose.py` variants B/C/H) — the equal-weighting of interchange/interest/reward/benefits/loss (the literal accounting equation) is itself ~5-10x worse than v19's CALIBRATED relative weights (f7=f1=0.738 dominant, f8=0.348, f6/f9/f10=0.06-0.137), independent of any sign question. Holding v19's exact weight RATIOS fixed and changing ONLY the travel sign to the brief's precise economics (5x reward > 2.2% interchange ⇒ negative) gives **+0.013** (`h3_isolate_v2.py` variant K) — small POSITIVE, not negative, confirming the prior v6/v8 LB losses were confounded by other simultaneous changes (A16 already suspected this). The literal 12% interest rate vs v19's tuned 0.738 f1-weight washes out exactly as expected under z-scoring (variant N == L exactly) — dollar RATES are irrelevant once re-normalized to relative importance; only the WEIGHT RATIOS matter. **Blend test (v19 + w·z(precise per-category net margin)): peaks at w=0.10-0.15 for +0.013 to +0.018 — POSITIVE but below the 0.020 noise floor, same magnitude as every other post-v19 probe (A27 G&S-share +0.0094, A28, A29).** | ✅ | 2026-07-01, `scratchpad/h3_exacteconomics.py` + `h3_decompose.py` + `h3_isolate_v2.py`. **HOLD v19 (0.859).** Durable writeup-grade fact: the issuer's literal accounting equation (interchange+interest-reward-benefits-loss, equal-weighted) is NOT how the hidden truth ranks members — relative term IMPORTANCE (spend/lending dominant, costs near-irrelevant at the margin, per A29) matters far more than getting the per-$ RATES exactly right. Precise economics is real for the Framework narrative (explains WHY f7/f1 dominate) but adds no ranking lever beyond v19; another confirmed-dead lever in the post-v19 exhaustion list (A27-A30). |

| A32 | **The 4th named driver "benefit utilization" (f13–f16) as a POSITIVE/engagement signal is DEAD — symmetric with A29's cost-side test.** 6 encodings (breadth count, magnitude sum, binary any/3+, spend-relative intensity, benefit×spend interaction) all peak at w=0 added to v19; every nonzero positive weight hurts monotonically from the first step (w=0.05 already −0.005 to −0.007). Mechanism: benefit usage is NOT orthogonal — it CORRELATES with v19's existing terms (Spearman 0.34 breadth / 0.50 magnitude with v19 score; 0.21 with spend), so it's a redundant restatement of the spend/engagement v19 already rewards, not new information; at the rank 90–110K boundary benefit breadth is flat (median 3 vs 3). With A29 (cost-side dead) + A28 (segment-merge dead), all three structurally-distinct probes of the 4th driver across BOTH signs are exhausted — benefit utilization is implicitly already captured by v19, not a separate lever. | ✅ | 2026-07-01, `scratchpad/h_benefitpos.py`. |
| A33 | **Fresh data forensics (22 scripts) found NO unexploited structure — and refuted the tie-break hypothesis at the root.** (1) TIE-BREAK DEAD: v19's boundary band rank 90–110K is **100% score-unique (zero ties)**; the exact cutoff value is held by 1 member. The big tie-groups (54K f3-evict floor, 42K no-bd zero) sit at the 81–100th pct, 345K ranks BELOW the cutoff — tie-breaking provably cannot move overlap (a sharper, stronger A19). (2) No decode clue: f5 re-confirmed orthogonal (best exact-rescale match 0.19%); every single-feature and 27 derived-ratio/missingness levers trace back to redundancy with f7/f1/catsum and HURT when screened. (3) Corrected facts for the WRITEUP (not levers): **f2/f3 are near-INDEPENDENT distress states (phi −0.159, not nested)**; **f17/f18 are NOT a strict subset (f18>f17 in 36.5%)** — two independently-measured lend lines (refines A4); **f1~f11 is inverted-U** (risk peaks at f1≈$3K, declines at extremes) → v19's f11·f1 loss term confirmed well-calibrated; **f16 entertainment-credit is CENSORED — 35.3% sit at the $64.4 cap** (5–13× any other feature) = a third of users fully exhaust the benefit (cost-side narrative support). All screened, none beats the 0.020 floor. | ✅ | 2026-07-01, `scratchpad/forensic_*.py` (22 files). |
| A34 | **THE ONE GENUINELY-UNTESTED, DEFENSIBLE, ORTHOGONAL LEVER LEFT = a BASIS TRANSFORM (rank/percentile-normalize inputs instead of std-scale), and our LB-predictor is STRUCTURALLY BLIND to it.** Framework-critic (reproduced every claim live) established: (a) our exhaustion is MORE airtight than memory credited — the 15,711 pure-transactor f7-whales we'd most suspect of over-promotion have a **0.97–1.00 in-rate across EVERY submission v5→v19 and are 100% in the consensus core** = our highest-conviction correct picks; v19 winsorized at q=0.99 is Jaccard 1.000 (we do NOT ride uncapped outliers); v19's marginal lift over v15 was just 988 high-balance revolvers (the lending recalib is real+narrow). Every reshuffling form DEMOTES the whales = the v6/v8/v16_log LB-REJECTED direction. (b) BUT the rank/basis-transform is the one structural axis NEVER LB-tested and not pre-condemned: full-rank form is Jaccard 0.605 / Spearman 0.890 vs v19 (swaps 24,594 boundary members). **lb_predict scores the rank-form at 0.688 — it CANNOT screen basis changes (it's fit to 13 std-scale subs → treats any basis change as "far→bad"), so this requires a REAL submission.** Proposal = HYBRID basis: keep f7+f1 on the dollar/std basis (linear-in-$ revenue → protects the whales), rank-normalize ONLY the small specialty cats f6/f8/f9/f10 (where std-scaling lets a moderate specialty spender masquerade as a whale). Honest odds (critic): **~35% real-and-reachable / ~65% public-overfit-that-regresses or a feature-decode we can't replicate.** → **CONFIRMED: v22 (rank f6/f8/f9/f10, keep f7/f1 dollar) uploaded 2026-07-01 → public LB 0.866, +0.007 vs v19 — WON, NEW BEST.** The critic's 35%-real landed; the basis axis is a genuine lever. Decisively validated the reframe: the LB-predictor rated v22 at 0.822 ("worse") because it is BLIND to basis changes → the ONLY way to find this was to spend a submission (downside zero via best-score-counts). Gain is clean (whales 0.999 in-rate). **The 0.859 "ceiling" was a limit of the linear-additive FORM, not the features — keep pushing the basis axis (stack margin-blend, alt partitions, re-tune for mixed basis; each is predictor-blind → submission-only).** | ✅ CONFIRMED | 2026-07-01, v22 LB 0.866. |

| A35 | **13/23 features (f1,f4,f5,f6–f10,f11,f12,f17,f18,f21) are independently two-sided winsorized at ≈[2.5,97.5] pctile** (exact-value plateaus at min/max holding 2.5–3.0% mass, smooth pre-cap spacing, near-zero cross-feature overlap of who's clipped). Truth was computed pre-clip ⇒ shared information ceiling; 42.6% of a proxy top-20% carries ≥1 capped feature ⇒ additive multi-feature scoring is structurally REQUIRED. Also: raddar-style additive jitter (std≈0.003) on 9 money features is real but rank-immaterial (no decode edge exists); **f16 = 64.403398·k/100 = "% entertainment credit used"** (decode `×1.552402`, rank-neutral, writeup-grade); f17/f18 are exact integers (clean); f5 orthogonal to catsum under every transform (final confirmation); data fully shuffled, no leaks. | ✅ | 2026-07-02 forensics (data-explorer, 11 scripts). |
| A36 | **Triangulation 2.0 (all 18 LB constraints, rich form family) reconstructs the hidden truth as a dollar-basis, lending-dominant P&L** — ratios seed-stable: f1/f7 ≈ 2.0–2.1, exp-loss/f7 ≈ 1.1, lodging/f7 ≈ 0.43, dining/f7 ≈ 0.25–0.29, airline ≈ 0, rank/dual terms ≈ 0, f3 demotion present (soft vs hard unidentified). 6-term restricted fit RMSE 0.0029 ≈ 15-term 0.0027; frontier LOO mean |err| 0.0035; posterior-E[LB] reproduces all scored anchors ±0.003. Decoded rates are economically coherent (revolve yield ~17–21%/$, LGD ~0.7–0.9, breakeven f11 ~0.26, airline 5x ≈ breakeven, lodging + via portal commission). **v22/v24's form wins were partial corrections toward this truth, not evidence of rank basis/interactions.** Honest limits: 15-dim family not uniquely identified (rank-heavy basins fit equally, REG-check); 4-at-once holdout under-predicts levels −0.04..−0.055 though ordering is exact ⇒ trust deltas/orderings, discount absolutes. | ✅ | 2026-07-02, `scratchpad/triangulate2.py` + `tri2_fit6.py` + `tri2_regcheck.py`; report `reports/gap-analysis-0.88-to-0.91.md`. |
| A37 | **Both internal screening instruments are structurally blind and produced false ceilings** — `lb_predict.py` scores overlap-with-our-own-consensus (v20 "flat" = CONFIRMED false negative: posterior E[LB] 0.875 ≈ v24's 0.877; v22 rated 0.822 scored 0.866), and A21's "LP-certified 0.823 ceiling" dissolved as data grew (884 membership patterns over 11 subs → **5,648 over 18**, median contested cell 2 members). The "whale-safety 0.999" doctrine is the same consensus artifact (whales = members of OUR every top-20%; the reconstruction says ~1.5% don't belong to the truth's). **Rule: never screen an orthogonal/far candidate with consensus-derived instruments; use the calibrated generator-fit posterior (A36) instead.** | ✅ | 2026-07-02. |

| A38 | **Raw-XML structural audit of all three challenge xlsx files is CLEAN — no side-channel data exists.** Data workbook: 1 sheet ("in"), dimension exactly A1:X500001 (24 cols), no hidden sheets/columns, no definedNames, no pivotCache/comments/revisions; generator "Microsoft Excel Online". Dictionary: A1:B25 exactly (23 features + id) — the ~7 undelivered attributes appear NOWHERE in the delivered artifacts. Template: 2 sheets as documented + inert customXml. Combined with A35 (rows shuffled, jitter decoded, full precision already parsed): **neither we nor the 0.925–0.929 pack can hold a data-artifact edge; the pack's edge must be computable from the same 23 clipped features (or be probing-information/shared-method).** | ✅ | 2026-07-02 late, stdlib zipfile+XML audit (session log). |
| A39 | **Winsor-plateau mass at v29's top-20% boundary is TINY and symmetric → the "un-clip the tails / within-plateau rescue" axis cannot fund a >0.915 candidate.** Band ranks 90–110K (n=20,000): only **194 f1-capped** members (110 just-in / 84 just-out); ≥1 value-feature-at-cap = 3,374 (1,626 in / 1,748 out — no tilt), concentrated on near-zero-truth-weight specialty spends (f6 770, f9 864, f5 633). The 2,642 f1-capped members v29 EXCLUDES are median rank 462,923, catspend 0, f11 0.150 = the distress profile v33 triple-validated the truth hard-evicts. Oracle ceiling on within-plateau reordering ≈ +0.002–0.003; any visible-feature proxy of pre-clip excess ≪ that. Extends the round-3 winsor-rescue null (frontier-LOO-neutral) with a direct mass measurement. `scratchpad/probe_winsor_boundary.py`. | ✅ | 2026-07-02 late (probe vs scores_v29). |

| A40 | **The utilization-basis lending axis (the last "canonical" out-of-family form hypothesis) FAILS DESIGN REVIEW under two honest implementations — killed at zero submission cost.** (1) Bounded within-cohort quantile replacing z(f1): crushes the dollar tail → swap profile demotes high-f1 boundary revolvers (f1 med $11.9K→$5.4K) = the thrice-LB-rejected v16_log/sqrt direction. (2) Dollar-preserving quantile-match selected by form-delta: promotes over-limit members (f1 $7.5K on $3.7K lines, catsum $5.5K, **f11 med 0.111** ≈ breakeven) while demoting clean big-line revolvers ($49K line, $54K spend, f11 0.004) — utilization is a DISTRESS proxy, not a profit signal, consistent with A17/A22/v33 (truth rewards LOW-risk revolvers, hard-evicts distress). ⇒ No defensible implementation exists; do not re-propose. The pack's ~+0.010 out-of-family edge remains unexplained after this; the only surviving out-of-family candidate is the nbd within-cohort reorder at held level (A15 imputer, OOS 0.46, never LB-tested at held level). `scratchpad/build_v36_probe.py` (both variants preserved in git history). | ✅ design-review | 2026-07-03. |

| A41 | **The no-breakdown cohort's top-quintile LEVEL is UNDER-DETERMINED by all 25 exact reads — the one measured degree of freedom left in the problem.** The 25 kept calibrations' own implied nbd levels span **991..3,644** (median 1,657; our banked set holds 1,740) — a ±700-member freedom band ≈ ±0.007 of overlap that no posterior refit can resolve (every calibration reproduces all reads at RMSE ≤0.004 regardless of its level). The posterior's vote-gap curve for a level-DOWN swap (weakest-vote in-top nbd → strongest-vote out-of-top non-nbd) is +0.17 at N=400, ~0 at N=800, negative beyond → a moderate demotion is favored even by the (echo-chamber-prone) posterior. Historical level tests only ever probed UP (v9, −0.041); DOWN was never tested. **Probe built: v37-L = v35 ⊕ (demote weakest-vote 500 nbd / promote strongest-vote 500 non-nbd), level 1,740→1,240; realized = 0.918971 + (r_repl − r_nbd)·0.005 ∈ [0.914, 0.924]; the 6-dp read measures the density differential exactly. submission-validator VERDICT: SHIP (all 8 checks exact: 500/500 demotions nbd, 500/500 promotions non-nbd f3=0, level exactly 1,240, byte-identical rebuild, sha256 fcc6c7e3…88f8).** Deployment: dummy bench (symmetric-sign probe → bench is the correct venue; promotion to primary only on a clear positive). | ⚠ probe SHIP-ready | 2026-07-03 late (session measurement; `scratchpad/build_v37L.py`). |

| A42 | **The public metric is an exact integer count over a denominator of 70,000** (top-20% of the 350K public rows): all 8 exact 6-dp readings (v34/v35/v36/v37L + the 4 pack leaders) sit within 0.03 of an integer×(1/70,000) (max rounding distance 0.035; chance ≈ 10⁻⁹). Implications: (a) per-reading sampling noise between full-population implied overlap and the public reading is **σ ≈ 0.001** (70/30 split), so the 15-term family's fresh in-sample RMSE 0.0037 across 27 reads is **positive proof the truth lies outside the family's span** (reduced χ²≈14, with 15 params already soaked); (b) a 500-member swap probe's net count carries **σ ≈ ±15 full-equivalent members** → the λ echo-shrink calibration points (0.75/0.12/0.21) are 1–2σ measurements (v37L's "+17 of 500" is 1.2σ from zero); treat λ as directional, not decimal-precise. | ✅ | 2026-07-03 red-team, `scratchpad/redteam_h_residual.py` §0; binomial-thinning arithmetic; `reports/redteam-2026-07-03.md`. |
| A43 | **The 27-read probe design never excited several feature directions — "axis closed" claims are family-conditional.** Capture-range audit (mean pct-rank of h within each of the 27 submitted top-100Ks): f7 spans 0.693–0.848 and f1 0.493–0.706 (well probed), but **f20 spans only 0.474–0.489 and f23 0.503–0.526** (≈ zero information bought, ever), f19 weakly (0.392–0.498); recent candidates are 0.975–0.995 mutually overlapping. A residual-vs-capture regression over all 27 exact readings (new instrument, kept) finds **no single never-modeled feature above the noise floor** (best: f5 at ρ=−0.41 vs a random-control floor |ρ|≈0.30–0.45) — so the missing out-of-family structure (proven by A42a) is either jointly-coupled small form distortions (dollar-basis/imputation/cap details) and/or terms in the unexcited directions; it is NOT a single large linear feature term. Composition tilts (weak, directional): the current set under-represents f19 (0.474) / f20 (0.484) / rewards-inactive members (34.6% in-set vs 51.4% pop); the never-in-any-top pool (272,024) tilts the other way. | ✅ | 2026-07-03 red-team, `scratchpad/redteam_h_residual.py` §2–4; `reports/redteam-2026-07-03.md`. |

| A44 | **The out-of-family structure behind the family's misfit (A42) is most consistent with a REWARDS-ENGAGEMENT COHORT — and the neat-generator-formula hypothesis is CLOSED NEGATIVE.** (a) Overnight sieve program: ~440K discrete business formulas (raw-dollar / globally-concave / per-feature mixed-basis grammars, each × cost/supp/rewards/regime side terms) evaluated against all 27 exact counts — best max\|err\| 0.0088 vs threading tolerance ±0.0025–0.003, and every concave/mixed cell fits WORSE than plain dollars → no round-rate generator exists in any searched grammar (`scratchpad/generator_sieve*.py`; consistent with pre-restart `sieve_v0`'s 0.0116). (b) Extending the 15-term family with z(f21), z(f4), z(f19), rank(f22) drops the 27-read fit from RMSE 0.0027–0.0047 to **0.0016–0.0020** — the four best calibrations in project history — with f21/f4/f22 POSITIVE at 100% sign agreement across kept fits (f19 ambiguous, 75%); CORE+[f21p,f4p] alone reaches 0.0021 (1/3 seeds); mrew (rewards-inactive demotion) is the only sign-stable single extra (`tri3_extras.py`, `tri3_joint2.py`). (c) The member-level residual-displacement fitter independently ranks f21-top swap-ins as the #1 correction direction (21.9% SS-reduction, above the 200-permutation null, rank 1 in 25/27 reading-jackknives; `redteam_h_residual*.py`). Caveats: extra params on 27 reads can partially overfit (the RMSE halving needs the full 4-term cluster at most seeds); era-fits wobble on WHICH cluster member leads. Only a designed read settles it — v39/v40/v41 were built + pre-registered for exactly that. **SETTLED by v41's read (2026-07-04, 0.917643, net −0.076±0.015): the STRONG form is dead — engagement swap-ins are boundary-PARITY, not boundary-beating; and at 28 constraints the cluster family's fit advantage vanishes entirely (seeds 0.0027–0.0036 ≈ the 15-term family's 0.0029; the 0.0016–0.0020 fits were the corner v41 falsified). The surviving residue: a PU-style core-resemblance classifier (`tiebreak_battery.py`, `tiebreak_signals.npz`) retrodicts all 6 non-f3 measured swap outcomes incl. uniquely calling v41 negative — validated as a TIE-BREAK (not swap driver) for v42's margin ordering.** | ✅ read | 2026-07-04; `reports/morning-brief-2026-07-04.md`, retrodiction table in session log. |

| A45 | **v41 (the A44 cluster family's own bounded trust-region update off v35, NOT a heuristic swap like v39/v40) is BUILT and its writeup drafted — construction + a genuine, disclosed cross-instrument disagreement, not yet resolved by an LB read.** Applies v34's rule with A44's 4 kept calibrations as the vote: swap-in requires unanimous 4/4 agreement (gap ≥60% of 4), cap 2,000 — exhausted at exactly **N=1,741** each way. Swap-in (f21 med 12,262; f4 med 190,062; f1/revolve med $0; catsum med $86,083; f11 med 0.0000) vs swap-out (f21/f4 med 0 — rewards-inactive; f1 med $38; catsum med $91,657; f11 med 0.0005) is the cleanest natural experiment yet: spend and risk are matched between the two groups, engagement is the one sharp axis of difference. Pre-registered box [0.9016, 0.9364] (= 0.918971 ± 1,741/100,000). **Load-bearing caveat for whoever reads this next:** the A44 extended posterior rates the SET IT ITSELF SELECTED at 0.937–0.938 — this is optimistic by construction (same 4 calibrations chose and grade it; a winner's-curse effect), NOT an independent forecast. Its anchor check (rating the UN-selected banked v35 set at 0.921 vs actual 0.918971, +0.002) bounds only the FLOOR of this bias — true self-selection optimism here is almost certainly larger. The UN-extended 15-term posterior (blind to all 4 new terms) rates v41's set at 0.913, BELOW v35's actual 0.918971 — the two instruments disagree and neither is a calibrated probability; only the pre-registered box actually bounds the risk. Do not quote 0.937–0.938 as an expected outcome in any future summary. | ⚠ built, LB read pending | 2026-07-04, `build_v41.py`; writeup `.claude/templates/framework-doc-v41-cluster.md`. |

| A46 | **Research round 3 (India + functional-form focus, 2026-07-04, 20+ sources): STRENGTHENS the decoded P&L on three fronts, adds NO ranking lever (all 3 actionable hypotheses tested and killed same-day via the retrodiction harness).** New verified anchors: (a) **Amex India (AEBC) FY24 audited financials** — fee:interest income ≈ 4:1 (₹15.28bn vs ₹3.76bn), write-off ≈2.15% (+0.27% fraud), GNPA 5.30%/NNPA 0.81%, MR liability ₹4.57bn, blended realized yield ≈8.2%/₹ (extract: `reports/aebc_fy24_extract.txt`); (b) India premium interchange 1.5–2.0% and Amex India APR 42%/yr sticker — our 1.3–2.2% interchange decode holds in BOTH markets, revolve decode ≈ risk/funding-adjusted realized yield; (c) lodging decode +8–9%/$ bracketed by external commissions (agent floor 10%, OTA 15–22%, Expedia blended 12.3%, Amex FHR supplier-commission T&C); (d) NY Fed SR 1143: interchange 1.82% vs rewards 1.57% of volume — the airline/dining ≈ 0 "wash" is literature-exact; (e) redemption→retention literature (Bond 2.3× defection for non-redeemers; McKinsey +25%; Dorotic 2014) predicts redeemers are MORE valuable prospectively — our v41 parity read means the organizers' truth books rewards as contemporaneous cost with NO retention-uplift term (writeup: strongest-alternative-tested framing); (f) CFPB superprime revolve ~20% + Amex 70:30 loans:receivables validate the ~60% f17/f18-missing = charge-only structure. **Hypothesis verdicts (retrodiction vs 7 measured swaps, `redteam_h_residual3.py`): H1 attrition-dampener 2/7 KILLED; H2a mid-risk hump KILLED (would promote the evicted elite-f3 pool); H2b risk-step NO-POWER (tail owned by the f3 screen); H3 India reweight = confirmed at the existing joint optimum.** | ✅ | 2026-07-04 web sweep (agent, cited); ranking unchanged, R2/writeup ammunition banked. |

| A47 | **The atom-algebra partial-identification LP (31 reads, EXACT public-70K basis, tight per-read tol) CANNOT localize the 5,672 misses — the counting channel is information-starved, not mis-tuned.** Ledger FEASIBLE ✓ (integrity: no read mis-decoded; all 31 mutually consistent at ±2 members for 6-dp reads / ±40 for 3-dp). 17,552 atoms; **dark mass** (271,807 members outside all 31 tops) ∈ **[0, 5662]** of 70,000; **misses inside probed territory** (outside v35, inside some other top) ∈ **[8, 5674]** — both span nearly the whole 5,672 miss budget, so the LP proves neither "the lever is dark" nor "re-rankable by blending." STRENGTHENS the 2026-07-02 `lp_localize` null: +8 reads, correct basis, and ±2 tol (vs ±150) did NOT tighten it → the looseness is fundamental, not a tolerance/basis artifact (matches the ~300-bit-banked-vs-361K-bit-object budget). **Intervention B — resolution-floor accounting (`probe_power.py`, δ_min=2.43/√k):** 9/11 LB swap-probes POWERED, genuinely closing their axes for edges >5–11% (f3 −0.61, supp-relationship −0.41, r4-bimodal −0.31, pu-similarity −0.29, nbd-ordering −0.25, r3 +0.28); only **v37L** (nbd-LEVEL, k=500, floor ±10.9%, measured +3.4%) and **v44** (calibrated tranche, k=533, floor ±10.5%, measured ≈0) are UNDERPOWERED — both immaterial (nbd cohort ~1,740 members; v44 = the same near-boundary pool 4 instruments already read at parity). Largest probe floor (k=7237 full rebuild) = 2.9%; largest TARGETED criticism probe (v43, k=2500) = 4.9% → **any <5% edge on an unprobed segment is invisible to every criticism probe fielded; resolving a 3.5–7% lever needs k≈1,200–4,800 swaps = 1 read.** ⇒ the ~560–700-member private gap is NOT re-rankable on current LB info and NOT one of the large axes we closed at power; the only residue is a small (<5%) lever in an UNPROBED segment (findable only by a big structured criticism probe — downside = 1 read if bankable-guarded, but the LP could NOT aim it) OR an out-of-family scoring model the 440K-formula sieve missed (A44, unreachable by any probe). | ✅ | 2026-07-05, `scratchpad/atom_lp.py` (+`atom_lp.log`) + `probe_power.py`; coherent with A21/A37/A42/A44 + the 2026-07-04 RESEARCH R4 "probing-dry" verdict. |

| A48 | **The f2 (cancellation-calls) axis is the largest genuinely-OPEN categorical axis — its historical closures don't hold up under the A47 audit standard, and the ledger cannot refute a hard evict.** Facts (`probe_triage.py`, 2026-07-05): f2 is BINARY (86,974 = 17.4% of pop; 0 NaN); f2∩f3 overlap only 99 members (near-independent, confirms A33); the modern top carries 2,415 callers (2.4%) with med catsum $94.2K / f1 $4.6K / f11 0.0007 (deep-top, incl. 237 consensus whales). **Every top since v10 has caller-share pinned in 2.37–2.75% (validator-corrected; ~384-member spread, v11min the 2.753% outlier → no modern read ever varied the axis at probe-resolvable scale; the 8.6pp historical spread is all v1–v9 confounded-era).** The 31-read LP: pop-wide f2 truth-mass ∈ **[0, 8,077]** of 70,000 → hard-evict FEASIBLE (not forced either). Prior closures re-audited: A21's dismissal = consensus-prob (A37-blind); R3's kill = a 31-member triple interaction (irrelevant to block rule); A46-H1's retrodiction 2/7 = unpowered (the 7 graded swaps never varied f2-share). **Probe built: v46 = v37L ⊕ 2,415-swap full evict, pre-registered [0.9022, 0.9360], powered at δ_min=4.9%.** → **SETTLED by the read (2026-07-05 13:46 IST, 0.911 display): net = −0.337 ± 0.028 → the evicted callers carry truth-density ≈ 0.86 = DEEP-TOP PARITY. The truth has NO attrition term: hard-evict refuted ~23σ; dampener bounded to φ = −2% ± 4% (zero). Twelfth axis closed POWERED — the one parity verdict in the project that cannot be a resolution-floor artifact. Our f2-exclusion (callers at full P&L parity) was correct all along; the blind family predicted the read at 0.906 through composition alone (residual +0.005). R2 exhibit: the truth hard-evicts involuntary distress exits (f3) but FULLY VALUES voluntary-attrition callers (f2) — profitability is booked contemporaneous, not survival-discounted (coheres with A46e: rewards booked as cost with no retention uplift).** | ✅ read | 2026-07-05; `probe_triage.py`, `build_v46.py`, read decode; [[experiment-history]] v46. |

## Forbidden assumptions (never make these)
- That `id` may be used as a predictor.
- That missing = 0 without a stated reason.
- That overfitting the public 70% is safe (hidden 30% decides).
- That a high-accuracy black box is acceptable without business logic.
- That objective weighting (CRITIC/entropy/PCA) can set the framework weights — with no label they reward dispersion, not profit (they up-weight `logins`/`email_open`); use as cross-check only. See `feature-notes.md` EDA round 2.
