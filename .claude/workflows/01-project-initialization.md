# Workflow 01 — Project Initialization

**Objective:** Stand up the workspace and restore full context so work can begin safely.
**Prerequisites:** Repo cloned; `docs/` challenge files present.

## Steps
1. Read `CLAUDE.md` and `.claude/README.md`.
2. Invoke **project-understanding**; skim all `memory/` files.
3. Confirm `.gitignore` covers `data/`, `submissions/`, generated `*.xlsx`, `__pycache__`.
4. Identify the current roadmap stage (`memory/roadmap.md`).

## Validation
- Can you state the objective, the §16 constraints, and the next roadmap step from memory?

## Deliverables
- A correct mental model; any stale memory corrected.

## Exit criteria
- Constraints internalized; next workflow selected.
