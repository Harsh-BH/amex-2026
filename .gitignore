# Data & generated artifacts — never commit the shared dataset or outputs
data/
submissions/
*.xlsx
!docs/*.xlsx          # the official challenge files are tracked (read-only)
reports/*.png

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Env / secrets
.env
